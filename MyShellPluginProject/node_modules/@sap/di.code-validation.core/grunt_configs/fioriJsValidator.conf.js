/**
 * Created by I028014 on 23/11/2016.
 */
module.exports = {
    voter: {
        mochaTest: {
            test: {
                options: {
                    reporter: 'mocha-junit-reporter',
                    reporterOptions: {
                        mochaFile: 'tests/fioriJsValidator/TEST-index.xml'
                    },
                    colors: true,
                    summery: true,
                    captureFile: 'tests/fioriJsValidator/output.txt', // Optionally capture the reporter output to a file
                    quiet: false, // Optionally suppress output to standard out (defaults to false)
                    clearRequireCache: false // Optionally clear the require cache before running tests (defaults to false)
                },
                src: ['fiorijsvalidator/test/**/*Spec.js']
            },
            src: ['fiorijsvalidator/test/*Spec.js']
        },
        coverage: {
            src: 'fiorijsvalidator/test/*Spec.js',
            colors: true,
            options: {
                timeout: 20000,
                print: 'summary',
                growlOnSuccess: true,
                excludes: ['fiorijsvalidator/customRules/*', 'fiorijsvalidator/test/data/*'],
                check: {
                    lines: 90,
                    branches: 70,
                    statements: 90,
                    functions: 90
                },
                coverageFolder: "coverage/fioriJsValidator",
                reportFormats: ['cobertura', 'html']
            }
        }
    }
}