/**
 * Created by I028014 on 23/11/2016.
 */
module.exports = {
    voter: {
        mochaTest: {
            test: {
                options: {
                    reporter: 'mocha-junit-reporter',
                    reporterOptions: {
                        mochaFile: 'tests/validationFramework/TEST-index.xml'
                    },
                    colors: true,
                    summery: true,
                    captureFile: 'tests/validationFramework/output.txt', // Optionally capture the reporter output to a file
                    quiet: false, // Optionally suppress output to standard out (defaults to false)
                    clearRequireCache: false // Optionally clear the require cache before running tests (defaults to false)
                },
                src: ['validationframework/test/**/*Spec.js']
            },
            src: ['validationframework/test/*Spec.js']
        },
        coverage: {
            src: 'validationframework/test/*Spec.js',
            colors: true,
            options: {
                timeout: 20000,
                print: 'summary',
                growlOnSuccess: true,
                //excludes: ['plugins/*'],
                check: {
                    lines: 90,
                    branches:85,
                    statements: 90,
                    functions: 90
                },
                coverageFolder: "coverage/validationFramework",
                reportFormats: ['cobertura', 'html']
            }
        }
    }
}