var fs = require("fs");

module.exports = {
	//nodejs: changed to read from fs instead of ajax
	_getRemoteJsonResource : function(sUrl){
		 var content = fs.readFileSync(sUrl);
		 return JSON.parse(content);
	}
}