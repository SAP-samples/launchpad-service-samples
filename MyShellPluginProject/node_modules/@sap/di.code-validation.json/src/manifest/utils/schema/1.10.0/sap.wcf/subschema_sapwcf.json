{
  "title": "JSON schema for SAP.WCF Namespace",
  "description": "Represents WCF Application specific attributes",
  "type": "object",
  "required": [ "appId", "appWindow"],
  "additionalProperties": false,
  "properties": {
   "_version": {
      "description": "Represents attributes format version. It is managed by namespace owner",
      "type": "string",
      "enum": ["1.0.0"]
    },
    "appId": {
      "description": "Represents the technical component name of the WCF application",
      "type": "string",
      "pattern": "^[a-zA-Z0-9\\/\\_]{1,30}$"
    },
    "appWindow": {
      "description": "Represents Window of the WCF application. E.g. MainWindow, SearchWindow...",
      "type": "string",
      "pattern": "^[a-zA-Z0-9\\/\\_]{1,70}$"
    },
    "appPlug": {
      "description": "Represents the Inbound plug for the WCF Application",
      "type": "string",
      "pattern": "^[a-zA-Z0-9\\/\\_]{1,30}$"
    }
  }
}
